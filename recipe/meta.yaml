{% set name = "napari-bbox" %}
{% set version = "0.0.6" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/napari-bbox-{{ version }}.tar.gz
  sha256: 8b70f2610da99185f51569447fc3b49f5d8afb5296cda37e29fb49771e747d7e

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.8,<3.11
  run:
    - magicgui
    - napari <0.4.18
    - npe2
    - numpy
    - packaging
    - pandas
    - python >=3.8,<3.11
    - qtpy
    - vispy

test:
  imports:
    - napari_bbox
  commands:
    - pip list
    - pip check
  requires:
    - pip
    - pyqt

about:
  home: https://pypi.org/project/napari-bbox
  summary: A new layer for bounding boxes in 2+ dimensions
  license: BSD-3-Clause
  license_file: LICENSE
  dev_url: https://github.com/bauerdavid/napari-bbox

extra:
  recipe-maintainers:
    - bauerdavid
    - goanpeca
    - jaimergp
